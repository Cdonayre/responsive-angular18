export enum ROUTE_PATHS {
  LOGIN = '/login',
  DASHBOARD = '/dashboard/main/init',
}

export enum STATUS_NAMES {
  NOT_FOUND = 404,
  SUCCESS = 200,
  SUCCESS_CREATED = 201,
  ERROR = 500,
  UNAUTHORIZED = 401,
}
